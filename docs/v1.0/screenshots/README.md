# Screenshots — pending

Not captured for v1.0. The Claude in Chrome browser extension wasn't
connected in the session where these docs were written, so screenshots
couldn't be taken.

To fill this folder in:

1. Install the Claude in Chrome extension:
   https://chromewebstore.google.com/detail/fcoeoabgfenejglbffodgkkbkcdhcgfn
2. Open the extension's side panel and sign in with the same account used
   here.
3. Ask Claude: "capture screenshots for the v1.0 docs" — it can navigate to
   the live app, log in, and screenshot each tab (Markets, Macro, News,
   Eligibility, and the login page).

Expected filenames once captured (referenced by the UI guide):

- `login.png`
- `markets-tab.png`
- `macro-tab.png`
- `macro-tab-no-key.png` (the setup-message state)
- `news-tab.png`
- `eligibility-tab.png`
- `eligibility-tab-blocked.png` (showing a BLOCKED result, e.g. India mutual fund)
